<Window x:Class="SmartTextEditor.EncodingSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择编码格式" Height="320" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DarkBackgroundBrush}"
        ResizeMode="NoResize">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="选择编码格式" FontSize="18" FontWeight="Bold" 
                       Foreground="{StaticResource DarkTextBrush}" HorizontalAlignment="Center"/>
            <TextBlock x:Name="CurrentEncodingText" Text="当前编码: UTF-8" 
                       FontSize="13" Foreground="{StaticResource DarkSecondaryTextBrush}" 
                       HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
        
        <!-- 编码选择 -->
        <Border Grid.Row="1" Background="{StaticResource DarkCardBrush}" 
                BorderBrush="{StaticResource DarkBorderBrush}" BorderThickness="1"
                CornerRadius="8" Padding="12">
            <ListBox x:Name="EncodingListBox"
                     Background="Transparent" Foreground="{StaticResource DarkTextBrush}"
                     BorderThickness="0" FontSize="14">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}" CornerRadius="6">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{StaticResource DarkSurfaceBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{StaticResource DarkAccentBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="{Binding Description}" FontSize="12" 
                                   Foreground="{StaticResource DarkSecondaryTextBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        
        <!-- 按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="✓ 确定" 
                    Style="{StaticResource ModernDialogButtonStyle}" 
                    Click="OK_Click" IsDefault="True"/>
            <Button Content="✕ 取消" 
                    Style="{StaticResource ModernCancelButtonStyle}"
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>