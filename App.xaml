<Application x:Class="SmartTextEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="Application_Startup">
    <Application.Resources>
        <!-- 现代化色彩系统 -->
        <SolidColorBrush x:Key="DarkBackgroundBrush" Color="#0D1117"/>
        <SolidColorBrush x:Key="DarkSurfaceBrush" Color="#161B22"/>
        <SolidColorBrush x:Key="DarkCardBrush" Color="#21262D"/>
        <SolidColorBrush x:Key="DarkAccentBrush" Color="#58A6FF"/>
        <SolidColorBrush x:Key="DarkAccentHoverBrush" Color="#79C0FF"/>
        <SolidColorBrush x:Key="DarkTextBrush" Color="#E6EDF3"/>
        <SolidColorBrush x:Key="DarkSecondaryTextBrush" Color="#7D8590"/>
        <SolidColorBrush x:Key="DarkBorderBrush" Color="#30363D"/>
        <SolidColorBrush x:Key="DarkSuccessBrush" Color="#3FB950"/>
        <SolidColorBrush x:Key="DarkWarningBrush" Color="#D29922"/>
        <SolidColorBrush x:Key="DarkDangerBrush" Color="#F85149"/>
        
        <!-- 按钮色彩系统 -->
        <SolidColorBrush x:Key="ButtonPrimaryBrush" Color="#238636"/>
        <SolidColorBrush x:Key="ButtonPrimaryHoverBrush" Color="#2EA043"/>
        <SolidColorBrush x:Key="ButtonPrimaryActiveBrush" Color="#1A7431"/>
        <SolidColorBrush x:Key="ButtonSecondaryBrush" Color="#21262D"/>
        <SolidColorBrush x:Key="ButtonSecondaryHoverBrush" Color="#30363D"/>
        <SolidColorBrush x:Key="ButtonSecondaryActiveBrush" Color="#282E33"/>
        <SolidColorBrush x:Key="ButtonDangerBrush" Color="#DA3633"/>
        <SolidColorBrush x:Key="ButtonDangerHoverBrush" Color="#F85149"/>
        
        <!-- 现代化主按钮样式 -->
        <Style x:Key="ModernPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonPrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.15"/>
                            </Border.Effect>
                            <ContentPresenter x:Name="contentPresenter"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"
                                            TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonPrimaryHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonPrimaryActiveBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#6E7681"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#484F58"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代化次要按钮样式 -->
        <Style x:Key="ModernSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonSecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter x:Name="contentPresenter"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"
                                            TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonSecondaryHoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource DarkAccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonSecondaryActiveBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 工具栏按钮样式 -->
        <Style x:Key="ToolBarButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernSecondaryButtonStyle}">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="3,2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <!-- 现代化文本框样式 -->
        <!-- 现代化文本框样式 -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="JetBrains Mono, Consolas, 'Courier New'"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="AcceptsTab" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="SelectionBrush" Value="{StaticResource DarkAccentBrush}"/>
            <Setter Property="SelectionOpacity" Value="0.3"/>
            <Setter Property="CaretBrush" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="4" ShadowDepth="1" Opacity="0.1"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                        HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource DarkAccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代化状态栏样式 -->
        <Style x:Key="ModernStatusBarStyle" TargetType="StatusBar">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#161B22" Offset="0"/>
                        <GradientStop Color="#0D1117" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代化菜单样式 -->
        <Style x:Key="ModernMenuStyle" TargetType="Menu">
            <Setter Property="Background" Value="{StaticResource DarkSurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ModernMenuItemStyle}"/>
        </Style>
        
        <!-- 现代化菜单项样式 -->
        <Style x:Key="ModernMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="6"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter ContentSource="Header"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkAccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkAccentHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代化状态指示器样式 -->
        <Style x:Key="ModernStatusIndicatorStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="LimeGreen" BlurRadius="6" ShadowDepth="0" Opacity="0.8"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代化编码标签样式 -->
        <Style x:Key="ModernEncodingLabelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource DarkAccentBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{x:Static SystemColors.WindowColor}" BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代化工具栏样式 -->
        <Style x:Key="ModernToolBarStyle" TargetType="ToolBar">
            <Setter Property="Background" Value="{StaticResource DarkSurfaceBrush}"/>
            <Setter Property="Height" Value="64"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        
        <!-- 现代化对话框按钮样式 -->
        <Style x:Key="ModernDialogButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernPrimaryButtonStyle}">
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <!-- 现代化取消按钮样式 -->
        <Style x:Key="ModernCancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernSecondaryButtonStyle}">
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Application.Resources>
</Application>